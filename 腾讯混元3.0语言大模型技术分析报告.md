# 腾讯混元3\.0（Hy3 preview）语言大模型技术分析报告

## 一、概述

2026 年 4 月 24 日，腾讯正式发布并开源了混元 3\.0 预览版（Hy3 preview）语言大模型。这是腾讯完成大模型底层技术全面重构后的首个成果，由前 OpenAI 研究员姚顺雨担任首席 AI 科学家主导开发。Hy3 preview 采用快慢思考融合的混合专家（MoE）架构，总参数量达 2950 亿，激活参数仅 210 亿，最大支持 256K 上下文长度。

发布仅两周，该模型在 OpenRouter 的全球 API 调用量排行榜上登顶总榜第一，Token 调用量较上代 Hy2 增长超 10 倍，代码与智能体场景增幅突破 16\.5 倍。Hy3 preview 标志着腾讯大模型战略从 "参数竞赛" 向 "实用导向" 的重大转变，确立了 "能力体系化、评测真实性、性价比追求" 三大核心原则。

## 二、模型结构分析

### 2\.1 整体架构

Hy3 preview 采用基于 Transformer 的混合专家（MoE）架构，这是当前大语言模型实现 "大参数、小激活" 的主流技术路线。与传统稠密模型不同，MoE 架构将模型拆分为多个 "专家" 网络，每个输入 token 只激活其中一小部分专家，从而在保持巨大知识容量的同时显著降低推理成本。

**核心架构参数**：

|参数项|数值|说明|
|---|---|---|
|总参数量|295B（2950 亿）|模型的整体知识容量|
|激活参数量|21B（210 亿）|每次推理实际计算的参数|
|激活比例|约 7\.1%|21B/295B|
|专家数量|192 个|模型内部的专家网络总数|
|每次激活专家数|8 个|每个 token 选择最相关的 8 个专家|
|模型层数|80 层 \+ 1 层 MTP|80 层 Transformer \+ 1 层多 Token 预测层|
|MTP 层参数|3\.8B|专门用于多 Token 预测的参数|
|注意力机制|GQA|分组查询注意力|
|查询头数|64 个|用于计算注意力的查询头|
|键值头数|8 个|用于存储键值对的头|
|头维度|128|每个注意力头的维度|
|上下文窗口|256K tokens|可处理约 20 万字的长文档|
|词表大小|120,832|模型支持的词汇数量|

### 2\.2 MoE 专家系统

Hy3 preview 的 MoE 专家系统采用了 192 个专家的大规模配置，每次推理仅激活 8 个专家。这种设计实现了知识容量与推理成本的解耦：295B 总参数确保了模型的知识广度，能够编码海量知识；而 21B 激活参数则保证了推理效率，每次推理只消耗相当于 21B 稠密模型的计算量。


### 2\.3 注意力机制

Hy3 preview 采用分组查询注意力（GQA）机制，将 64 个查询头分为 8 组，每组共享一个键值头。这种设计在保持多头注意力性能的同时，显著降低了 KV 缓存的内存占用。


配套 KV 缓存与注意力加速优化技术

- 跨层注意力（CLA）推理缓存优化
推理阶段启用跨层 KV 共享策略，每两层 Transformer 复用同一套 KV 激活缓存，与 GQA 形成双重压缩，进一步降低长文档场景显存开销。

- Stem 块级稀疏注意力算子加速（推理侧）
搭载腾讯自研 Stem 稀疏注意力算法，基于因果信息流做块级权重筛选，仅保留 25% 有效注意力计算路径；配套专用 HPC 算子，在 256K 超长上下文场景下显著降低算力消耗。

### 2\.4 多 Token 预测（MTP）层

Hy3 preview 在传统 Transformer 架构基础上增加了一个 3\.8B 参数的多 Token 预测（MTP）层。这一创新设计允许模型在一次前向传播中预测多个连续的 token，而不仅仅是下一个 token。

**MTP 层的优势**：

- 投机解码模式下大幅减少解码步数、显著提升生成吞吐与推理速度

- 在代码、结构化 JSON、工具调用等规律输出场景降低单位 token 计算开销

- 原生适配 vLLM、SGLang 等主流推理框架的 MTP 投机解码流程

## 三、核心技术创新点

### 3\.1 快慢思考融合推理机制

Hy3 preview 最引人注目的创新是快慢思考融合（Fast \& Slow Thinking Hybrid）机制，这一设计灵感源自心理学家丹尼尔・卡尼曼的双系统理论。

**三档推理模式**：

|模式|参数值|适用场景|行为特征|
|---|---|---|---|
|直接响应|`"no_think"`|简单问答、闲聊、格式转换|直接生成答案，无额外推理步骤|
|低强度推理|`"low"`|中等复杂度任务|有限的推理步骤，平衡速度与质量|
|高强度推理|`"high"`|数学、编程、复杂推理|深度链式推理，最大化推理质量|

**技术优势**：

- 实现了智能密度的最大化

- 在相同计算成本下获得更高的智能输出

- 用户可根据任务复杂度灵活选择推理深度

- 推理效率相比上代模型提升 40%

### 3\.2 Agent\-first 训练范式

姚顺雨主导的混元团队彻底重建了预训练和强化学习基础设施，提出了 "Agent\-first" 的训练范式，这是 Hy3 preview 在智能体能力上取得突破的关键。

**训练体系重建**：

- 新成立 AI Infra 部、AI Data 部和数据计算平台部

- 构建了专门针对智能体任务的训练框架

- 支持多轮工具使用、代码执行和复杂工作流的训练

**强化学习优化**：

- 采用基于 GRPO 框架的增量式、领域聚焦的 RL 流程

- 两阶段 GRPO 训练策略：先优化推理领域（逻辑、编码、数学、科学），再扩展到通用任务

- 在 Token 级别重新制定 GRPO 损失，显著提高 KL 稳定性

### 3\.3 混合精度 KV Cache 压缩技术

Hy3 preview 在生产环境中首次启用了 FP8\+INT4 混合量化的 KV Cache 压缩技术，并采用了创新的 "分层保真策略"。

**分层保真策略**：

- 对 attention 层输出的前 10% token 保持 FP16 精度，保障首句生成质量

- 中间 50% 使用 FP8 精度，平衡质量与效率

- 尾部 40% 使用 INT4 精度，最大限度压缩显存占用

### 3\.4 模型与推理框架深度协同优化

Hy3 preview 实现了模型架构与推理框架的深度协同设计，这是其推理效率提升 40% 的重要原因。

**协同优化措施**：

- 算子性能提升：针对 MoE 架构和 MTP 层优化底层计算算子

- 量化算法优化：开发了专门针对 Hy3 架构的量化算法，降低精度损失

- 与 vLLM、SGLang 等主流推理框架深度集成，支持 MTP 投机解码
